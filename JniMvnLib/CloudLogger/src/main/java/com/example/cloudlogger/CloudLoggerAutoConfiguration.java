package com.example.cloudlogger;

import org.springframework.boot.autoconfigure.AutoConfiguration;

@AutoConfiguration
public class CloudLoggerAutoConfiguration {
    // Auto-configuration marker. Native bootstrap happens in ApplicationContextInitializer.
}
